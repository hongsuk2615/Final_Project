<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="boardMapper">

	<resultMap type="board" id="boardResultSet">
		<id column="BOARD_NO" property="boardNo"/>
		<result column="CATEGORY_S_NO"  property="categorySNo"/>
		<result column="CONTENT"  property="content"/>
		<result column="CREATE_DATE"  property="createDate"/>
		<result column="READ_COUNT"  property="readCount"/>
		<result column="REPORT_COUNT"  property="reportCount"/>
		<result column="TITLE"  property="title"/>
		<result column="USER_NO"  property="userNo"/>
		<result column="WISH_COUNT"  property="wishCount"/>
	</resultMap>
	
	<resultMap type="upperCategory" id="upperCategoryResultSet">
		<id column="CATEGORY_U_NO" property="categoryUNo"/>
		<result column="CATEGORY_U_NAME" property="categoryUName"/>
		<result column="CATEGORY_PATH" property="categoryPath"/>
	</resultMap>
	
	<select id="selectBoard" resultMap="boardResultSet">
		SELECT *
		  FROM BOARD
		 WHERE BOARD_NO = 1
	</select>
	
	<insert id="insertBoard" useGeneratedKeys="true" parameterType="board" keyProperty="boardNo">
		INSERT INTO BOARD (BOARD_NO, CATEGORY_S_NO, CONTENT, CREATE_DATE, READ_COUNT, REPORT_COUNT , TITLE, USER_NO, WISH_COUNT)
		VALUES ( default, #{categorySNo}, #{content}, #{createDate}, #{readCount}, #{reportCount}, #{title}, #{userNo}, #{wishCount})
	
	</insert>
	<select id="selectBoardListCount" resultType="_int">
		SELECT COUNT(*)
		FROM BOARD
		WHERE BOARD_
	</select>
	
	<insert id="insertBoard">
		INSERT INTO BOARD
		(
		CATEGORY_S_NO,
		TITLE,
		CONTENT,
		USER_NO
		
		)
		VALUES(
		2,
		#{title},
		#{content},
		#{userNo}
		)
	</insert>
	<select id="selectCategoryList" resultMap="upperCategoryResultSet">
		SELECT *
		  FROM UPPER_CATEGORY;
	</select>
	
</mapper>
